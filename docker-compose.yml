services:
  mikrodash:
    build: .
    container_name: mikrodash
    env_file: .env
    ports:
      - "3081:3081"
    volumes:
      - mikrodash-data:/data
    restart: unless-stopped

volumes:
  mikrodash-data:
